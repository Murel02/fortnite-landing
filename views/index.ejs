<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Fortnite Random Drop</title>
    <style>
      .map-container {
        position: relative;
        width: 650px; /* adjust to match your actual map size */
        margin: auto;
      }
      .map-container img {
        width: 100%;
      }
      .marker {
        position: absolute;
        background: red;
        width: 12px;
        height: 12px;
        border-radius: 50%;
        transform: translate(-50%, -50%);
      }
    </style>
  </head>
  <body>
    <div
      class="marker"
      id="marker"
      data-x="<%= location.x %>"
      data-y="<%= location.y %>"
    ></div>
    <p>Your random drop location is: <%= location.id %></p>
  </body>

  <script>
    const marker = document.getElementById("marker");
    const x = parseFloat(marker.dataset.x);
    const y = parseFloat(marker.dataset.y);
    marker.style.left = x * 100 + "%";
    marker.style.top = y * 100 + "%";
  </script>
</html>
